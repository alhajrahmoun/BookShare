<%= stylesheet_link_tag 'community-style', media: 'all' %>
<div class="container">
        <div class="row">
            <div class="col-md-12">
                <a href="#" class="logo1"></a>
            </div>
        </div>
</div>

<div class="container">
            <nav class="navbar navbar-default" role="navigation">
                <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                    <%= link_to "Home", root_path, class: "navbar-brand"%></li>
                    </div>
                
                <div class="collapse navbar-collapse navbar-ex1-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active"><%= link_to "Home", root_path%></li>
                        <li><%=link_to "Communities", communities_path %></li>
                        <% if user_signed_in?%>
                        <li><%=link_to "Profile", user_path(current_user) %></li>
                      
                         <li><%=link_to "Sign Out", users_sign_out_path%></li>
                          <% else %>
                        <li ><a href="#">Contact Us</a></li>
                        <li ><a href="#">About</a></li>
                        <% end %>
                    </ul>
                </div>
            </nav>
            <div class="col-md-6 col-md-offset-3">
            <h3>Follow communities to get updates</h3>
            <div class="addbook">
            <% if user_signed_in?%>
            <%= link_to "Add Book", new_book_path, class: 'btn btn-primary col-md-offset-2' %>
            <%= link_to "next", home_index_path, class: 'btn btn-primary col-md-offset-3' %>
            <% end %>
            </div>
            </div>
            

            <div class="row">
            <% @communities.each do |community| %>
                <div class="comm">
            <div class="col-md-6">
                <%= render community %>
            </div>
                </div>
        <% end %> 

</div>

</div>